@model IEnumerable<Bibliotech.Models.Categoria>

@{
    ViewData["Title"] = "Categorias";
}

<div class="d-flex justify-content-between align-items-center">
    <h1 class="inter-bold">Categorias</h1>
    <a asp-action="Create" class="inter-bold btn-sm btn-custom btn-custom-bg-blue">Nova Categoria</a>
</div>
<table class="table inter-regular">
    <thead>
        <tr>
            <th class="inter-bold">
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategoriaId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.CategoriaId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoriaId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
